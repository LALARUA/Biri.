server:
  port: 8002
spring:
  application:
    name: login-register
  redis:
    host: 10.8.162.219
    port: 6379
  session:
    store-type: redis
eureka:
  instance:
    prefer-ip-address: true #注册服务时使用服务的ip地址
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
#feign:
#  httpclient:
#    enabled: true